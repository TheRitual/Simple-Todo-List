import "./style.css";

const Form = () => (
    <form>
        <input className="form__input" placeholder="What to do?" />
        <button className="form__button">Add Task</button>
    </form>
);

export default Form;